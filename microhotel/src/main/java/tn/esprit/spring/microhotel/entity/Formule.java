package tn.esprit.spring.microhotel.entity;

public enum Formule {
    CHAMBRE_SEULE,        // juste la chambre
    DEMI_PENSION,         // chambre + petit-déj + dîner
    PENSION_COMPLETE      // chambre + petit-déj + déjeuner + dîner
}
